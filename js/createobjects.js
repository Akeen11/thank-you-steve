const createSnackCake = (title, description, amount, wrapping, weight) => {
    const snackCake = {
        title: title,
        description: description,
        amount: amount,
        wrapping: wrapping,
        weight: weight,
    }
    return snackCake
}